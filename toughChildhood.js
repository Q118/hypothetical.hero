/** @format */
function analyze(params) {
	analyze(params);
	//? return params
}
function toughChildhood(hero) {
	const life = {};
	life.name = hero;
	life.environmentalFactors = [];
	life.stuffBornWith = [];
	life.theme = function () {
		while (age < 18) {
			analyze(
				cliffNotes(life.experiences),
				life.stuffBornWith,
				life.environmentalFactors
			);
		}
	};
	const herosChildhood = life.analyze(
		environmentalFactors,
		stuffBornWith,
		theme
	);
	return herosChildhood;
}
